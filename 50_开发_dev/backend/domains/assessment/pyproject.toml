[project]
name = "family-assessment-domain"
version = "0.1.0"
description = "Assessment domain (UI-02/UI-03) — ported from apps/api/src/modules/family/{assessment,growth-hypothesis,safety-assessment.policy}.service.ts"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9", "sqlalchemy>=2.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
