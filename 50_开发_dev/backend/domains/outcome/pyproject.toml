[project]
name = "family-outcome-domain"
version = "0.1.0"
description = "Outcome domain (Batch 2) — ported from apps/api/src/modules/family/growth-review.service.ts"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9", "sqlalchemy>=2.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
