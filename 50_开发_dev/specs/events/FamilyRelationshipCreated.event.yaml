version: 0.1
event: FamilyRelationshipCreated
immutable: true
required_fields:
  - event_id
  - family_id
  - relationship_id
  - occurred_at
  - actor_id
  - correlation_id
metadata_required:
  - source
  - schema_version
