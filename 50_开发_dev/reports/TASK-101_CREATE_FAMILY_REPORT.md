# TASK-101 CreateFamily Report

as_of: 2026-08-09

## Verdict

TASK-101_CREATE_FAMILY: **PASS**

## Scope

Implemented the approved `CreateFamily` Named Action only.

Out of scope objects were not created: GrowthProfile and Membership remain untouched.

## Implementation

| Area | Result |
|---|---|
| API route | `POST /families` |
| Schema validation | rejects non-object input, unknown fields, invalid display name, invalid idempotency key, and client-supplied `family_id` |
| Auth precondition | requires `x-actor-id`; missing actor returns unauthorized |
| Transaction boundary | one PostgreSQL transaction covers idempotency lock, family insert, audit insert, outbox insert, idempotency response update |
| Idempotency | same key + same request replays response; same key + different request returns conflict |
| Audit | writes `audit_logs` with actor, correlation id, idempotency key, resource id, and metadata |
| Domain event | writes `outbox_events` with `FamilyCreated` payload and required metadata |
| Server id generation | `family_id` generated by PostgreSQL `gen_random_uuid()` |

## Acceptance Criteria

| AC | Result | Evidence |
|---|---|---|
| AC1 valid request creates one Family | PASS | `family.service.integration.spec.ts` checks exactly 1 row in `families` |
| AC2 family_id is server generated | PASS | DTO rejects supplied `family_id`; integration test asserts UUID returned from DB |
| AC3 duplicate idempotency key does not create duplicate Family | PASS | integration test replays same response and still has 1 family |
| AC4 FamilyCreated event exists | PASS | integration test checks `outbox_events.event_name = FamilyCreated` |
| AC5 audit record exists | PASS | integration test checks `audit_logs.action_name = CreateFamily` |
| AC6 client-supplied family_id is rejected | PASS | unit test rejects `family_id` |
| AC7 no GrowthProfile/Membership is created | PASS | integration test asserts `growth_profiles` remains empty; no Membership implementation exists |

## Validation

Commands run:

```powershell
$env:DATABASE_URL='postgres://family:***@localhost:55433/family_gate'
pnpm --filter @family/api test
pnpm --filter @family/contracts build
pnpm --filter @family/api typecheck
pnpm --filter @family/api lint
pnpm --filter @family/api build
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

Results:

- `pnpm --filter @family/api test`: PASS, 4 files, 6 tests, including PostgreSQL integration tests.
- `pnpm --filter @family/api typecheck`: PASS after rebuilding `@family/contracts`.
- `pnpm --filter @family/api lint`: PASS.
- `pnpm --filter @family/api build`: PASS.
- `pnpm typecheck`: PASS, 3/3 turbo tasks.
- `pnpm lint`: PASS, 2/2 turbo tasks.
- `pnpm test`: PASS, 3/3 turbo tasks. Note: turbo did not propagate the shell `DATABASE_URL` into the API test task, so the PostgreSQL integration spec was skipped in this aggregate run; it passed in the direct filtered API test above.
- `pnpm build`: PASS, 2/2 turbo tasks.

## Files Changed

- `apps/api/package.json`
- `apps/api/src/modules/family/create-family.dto.ts`
- `apps/api/src/modules/family/create-family.dto.spec.ts`
- `apps/api/src/modules/family/family.controller.ts`
- `apps/api/src/modules/family/family.controller.spec.ts`
- `apps/api/src/modules/family/family.module.ts`
- `apps/api/src/modules/family/family.repository.ts`
- `apps/api/src/modules/family/family.service.ts`
- `apps/api/src/modules/family/family.service.integration.spec.ts`
- `packages/contracts/src/index.ts`
- `pnpm-lock.yaml`

## Residual Risks

- HTTP status decoration for create currently returns the Nest default for POST when invoked through HTTP. Service/controller unit and integration behavior are verified; a future E2E HTTP test should lock the exact wire status once the app test harness is introduced.
- Aggregate turbo tests skip DB integration when `DATABASE_URL` is not propagated into the task environment. Direct filtered API test with `DATABASE_URL` remains the authoritative TASK-101 DB test evidence.

## Decision

TASK-101 is complete. Stop before TASK-102 unless explicitly directed.