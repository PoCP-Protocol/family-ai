# Family Web Browser Smoke Findings 001

## 2026-08-17：本地静态 34 页体验验证

验证入口：`http://localhost:5173/?product=test-loop`。

| 路径 | 观察结果 | 状态 |
|---|---|---|
| 首页（UI-01） | 家庭成长平台原图母版正常显示；“立即开始测评”、AI 成长说明、挑战营、计划、案例、名师、任务、内容卡和底部导航均存在可点击热点。 | MATCHED |
| 首页 → 家庭成长体检入口（UI-02） | 点击首页“立即开始测评”后进入含“3分钟了解孩子成长状态”和“立即开始测评”的原图母版页面。 | MATCHED |
| 体检入口 → 测评第二步（UI-03） | 点击 UI-02 原图“立即开始测评”热点后，页面通过 Web Gateway 入口尝试请求服务端；本地未启动 API 时进入 UI-03 原图，页面抽取到固定文本等价：`当前说明暂不可用。你可以返回、暂停或现在先不继续。` | MATCHED（客户端故障的安全回退） |

本轮浏览器未注入真实 LLM key，未声称真实模型已调用。API integration 已单独证明：配置缺失时服务器返回 `BLOCK_CONFIGURATION / LLM_DISABLED / draft=null`、保存最小审计且零领域状态写入。

## 视觉与文案检查

浏览器页面未出现 DEV、stub、Gate、policy、contract 等开发/治理内部术语。可见视觉继续使用原始素材文件路径；这一历史路径将在后续命名补丁中通过 alias mapping 说明，不作机械改名。

| 核心服务续程（UI-03 → 报告 → 计划） | 点击 UI-03“下一步”后可到达家庭成长报告，再点击“生成我的成长计划”可到达 21 天亲子沟通挑战营计划页；报告、计划、任务入口和底部导航均仍可见。 | MATCHED |

> 浏览器页面同时保留了 fail-closed 文本等价。当前静态服务器未与 API 服务共同启动，所以本轮为客户端连接失败回退；它不替代已通过的 PostgreSQL API `LLM_DISABLED` 集成证据。

| 商城闭环直达页（UI-13） | 使用 `?product=test-loop&page=commerce-mall` 成功呈现商城原图母版。DOM 已渲染 `commerce-invite` 与三个 `commerce-product` 热点；浏览器可见元素扫描未标注透明热点，需在后续 CSS 核验中补充相应的几何定位规则。 | IN_RESEARCH |
| 商城闭环（UI-13 → UI-14） | 补齐透明命中区后，浏览器可识别“立即邀请”和三个商品热点。点击 21 天亲子沟通挑战营后进入商品详情原图，并识别“发起拼团”“去分享”动作。 | MATCHED |
| 名师服务闭环（UI-19 → UI-20） | 通过 `?page=teacher-zone` 直接加载名师专区原图；浏览器识别“查看名师详情”热点，点击后进入名师详情原图，并显示“预约一对一”入口。 | MATCHED |
| 名师预约入口（UI-20 → UI-21） | 点击“预约一对一”后进入在线咨询/预约原图；本地未连 API 时同时出现“可以返回、暂停或现在先不继续”的文本等价回退。 | MATCHED（客户端故障安全回退） |
| 家长社区闭环（UI-25） | 使用 `?page=parent-community` 可直接打开家长社区原图；浏览器识别查看动态详情、发布动态、我的社区三处透明热点。 | MATCHED |
| 社区发布入口（UI-25 → UI-26） | 点击发布动态后进入发布动态原图，并在 API 不可达时保留固定文本等价回退；未发生社区外发。 | MATCHED（客户端故障安全回退） |
| 客户后台/会员资产（UI-30） | 使用 `?page=annual-member-mine` 直达年度会员服务原图，浏览器识别“查看我的服务”入口。 | MATCHED |
| 客户后台续程（UI-30 → UI-31/33） | 点击“查看我的服务”后可到达家庭档案原图，并保留无 API 时的文本等价回退；未触发真实会员权益或档案写入。 | MATCHED（客户端故障安全回退） |
| 线下沙龙闭环（UI-22） | 使用 `?page=salon-list` 直达线下沙龙原图，浏览器识别“查看活动详情”热点。 | MATCHED |
