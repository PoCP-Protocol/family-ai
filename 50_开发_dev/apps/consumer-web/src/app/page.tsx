import Link from 'next/link';
import { consumerRoutes } from '@family/web-platform';

const nav = [['今天','/today'],['成长','/growth/assessment'],['发现','/discover'],['服务','/services'],['我的','/mine/family']];
export default function HomePage() { return <div className="shell"><aside className="rail"><Link className="brand" href="/">Family<small>家庭成长工作台</small></Link><nav className="nav">{nav.map(([label, href]) => <Link href={href} key={href}>{label}</Link>)}</nav></aside><main className="main"><header className="topbar"><input className="search" placeholder="搜索任务、服务和家庭小记" aria-label="搜索" /><span className="eyebrow">当前家庭</span></header><section className="hero"><span className="eyebrow">今晚一件事</span><h1>先连接，再行动</h1><p>这里会显示当前家庭范围内的真实投影。连接家庭会话后，才能看到今天的任务与计划状态。</p><Link className="button" href="/login">登录并选择家庭</Link></section><section className="grid"><article className="card"><span className="eyebrow">今天</span><h2>还没有加载家庭行动</h2><p>登录后读取 UI-01 家庭首页和今日行动。没有真实数据时，不显示演示数字。</p></article><article className="card"><span className="eyebrow">家庭边界</span><h2>家庭私有、可回看</h2><p>不生成家庭总分、孩子排名或未经确认的成长结论。</p></article></section><small hidden>{consumerRoutes.join('|')}</small></main><aside className="context"><span className="eyebrow">家庭上下文</span><h3>尚未选择家庭</h3><p>登录后根据服务端会话显示可访问的家庭，不接受 URL 中的 familyId 作为授权依据。</p><h3>法咪莉校长</h3><p>场景内辅助只在明确提交后读取最小化上下文。</p></aside></div>; }
