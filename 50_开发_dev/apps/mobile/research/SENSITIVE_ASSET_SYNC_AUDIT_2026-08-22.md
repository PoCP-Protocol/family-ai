# Family AI 敏感资产同步审计记录

**日期：** 2026-08-22

## 审计结论

本次审计针对受管移动项目中的环境文件、开发日志、构建缓存和截图资产。同步遵循“仅推送可公开、可复现、无凭据且与产品源代码直接相关的资产”原则。原始环境配置、运行日志、构建缓存和浏览器会话截图均不进入 GitHub；这些文件可能包含本机路径、开发会话状态、家庭体验数据或未来运行时配置，且不能作为稳定源代码资产。

| 资产类别 | 审计结论 | GitHub 处理 | 原因 |
|---|---|---|---|
| 环境文件 | 不纳入 | 排除 | 即使是开发配置，也可能包含服务地址、会话开关或后续密钥；不复制原始值 |
| 开发日志 | 不纳入 | 排除 | 可能包含请求上下文、设备/本机路径及暂态运行信息，且不具可复现性 |
| Expo/依赖缓存 | 不纳入 | 排除 | 可由依赖清单重建，体积大且易随机器变化 |
| 浏览器会话截图 | 不纳入 | 排除 | 可能展示家庭数据、会话状态或过期运行界面，不作为产品基线 |
| 35 UI 原图基线 | 已追踪 | 不重复推送 | 仓库中已有 73 个移动基线资产，继续作为视觉核对依据 |
| 受控应用图片资源 | 已追踪 | 不重复推送 | 仓库中已有 16 个移动图片资源；源代码引用的资源仍按正常代码变更同步 |

## 本轮安全同步资产

本次新增并可安全公开的资产仅为本审计记录及其任务台账状态。它们不含环境变量值、令牌、原始日志、缓存或任何浏览器会话截图。

> 本记录不表示环境、日志、缓存或截图已经推送；相反，它明确记录这些原始资产被安全排除。若后续确有公开诊断需要，应从源码和已发布基线重新生成最小化、脱敏的材料，而不是提交运行目录。
