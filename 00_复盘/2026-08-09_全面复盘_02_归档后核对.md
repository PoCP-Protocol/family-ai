# 阶段性全面复盘（第 2 次）｜2026-08-09 归档后核对

范围:`D:\Family` 全部文档与代码。
方法:逐文件读完 + 跑代码 + 哈希/差分实测,不靠印象;不复述前一版,只核对其之后的实况。
前一版:`2026-08-09_全面复盘.md`(16:49,聚焦"归档整理")。此后根目录多个文件在 16:52–17:07 又被改动。

**结论先行:归档结构确实立住了,知识层装置完好、卡片库仍空(符合预期,非退化)。但本次实测查出两处前一版未覆盖的结构性隐患 —— 权威规格被复制成两份、整个仓库无版本控制 —— 加上三笔旧债一笔未动。这些在"补第一张卡片"之前就该先处理,否则会重演"两套并存"。**

---

## 一、实测核对表(全部现跑,非引用)

| 核对项 | 命令/方法 | 结果 |
|---|---|---|
| 知识层可导入 | `Library.load()` | ✅ 正常 |
| 卡片总数 | `len(lib.cards)` | **0**(五层皆 0) |
| 校验输出 | `lib.validate()` | `issues: []` —— **空库无可校验,不代表健康** |
| yaml 骨架 | 读五个 `library/*.yaml` | ✅ 均为 `layer: []` + 模板注释,且回指 `ISSUES.md` 的对齐缺口(methods→B1、modalities→B3),做得好 |
| 引文核验装置 | 读 `citations.py` | ✅ 离线返回 `offline`、不伪造成功;逻辑完整 |
| 证据门 | 读 `evidence.py` | ✅ E0–E7 + Provenance + `NON_DECISIVE` 门齐全 |
| 规格副本一致性 | `diff -rq docs/impl-v1.0 10_规格_spec` | ⚠️ **仅差 `ISSUES.md`,其余逐文件相同 → 见下 §二.1** |
| 版本控制 | `git status` | ⚠️ **fatal: not a git repository → 见下 §二.2** |

结论:代码侧与前一版一致,无退化;**结构侧新增两个隐患**。

---

## 二、本次新发现(前一版未覆盖)

### 1. 权威规格被复制成两份 —— 第二处"两套并存"

`docs/impl-v1.0/` 是 `10_规格_spec/` 的**逐文件副本**,除了少一个 `ISSUES.md`,其余 11 个文件全部相同(`01`–`06` + csv + README + START_HERE + manifest)。

这正是这次归档想根除的病:前一版消灭了"素材两套编号",却在根目录留下了"规格两份副本"。危害是实打实的:

- CLAUDE.md 宣布 `10_规格_spec/` 是唯一权威,但磁盘上有第二份长得一模一样。任何人(或未来的我)编辑 `docs/impl-v1.0/` 都会与权威源静默漂移,而副本里**没有 ISSUES.md**,读到它的人会以为规格无冲突。
- `docs/impl-v1.0` 是一个**过期快照**(定格在带冲突却未登记的状态)。
- **建议**:`docs/impl-v1.0/` 只有两条路 —— (a) 删除,权威只留 `10_规格_spec/`;(b) 若要留作"v1.0 冻结版"存档,移进 `90_归档_archive/` 并在其 README 标明"历史快照,不作权威、不再更新"。**未擅自删,等你定。**

### 2. 整个 `D:\Family` 不是 git 仓库 —— 无任何版本控制

前一版反复说"所有搬移都是 move,可逆""未擅自改 spec""改动需变更评审" —— 但这些承诺**没有任何机制托底**:没有 git,就没有历史、没有 diff、没有 blame、没有回滚。

- "规格一字未改"目前只能靠自觉,无法证明;下一次编辑 spec 到底改了什么,无从追溯。
- 归档那些 `move` 一旦出错,不可恢复。
- `06` 门禁清单里的 Architecture Review / Release Gate 全部预设有版本控制承载评审。
- **建议**:`git init` + 一次基线提交,把 `90_归档_archive/`(47M 可再生物)和 `__pycache__/` 写进 `.gitignore`。这是**开工前的第 0 项**,成本极低、收益极高。**未擅自执行。**

### 3. 悬空引用仍在:`25_研究_research/docs/GUARDRAILS.md`

根 `README.md`、`25_研究/README.md`、`BACKLOG.md` 三处都把它当红线出处引用,但文件**不存在**。BACKLOG 已把它列为"建设顺序第 1 步(不依赖任何代码)"。这是当前**唯一一处零成本即可消除的诚实性缺口**,建议尽快补上再谈其它。

---

## 三、三笔旧债现状:一笔未动(符合"待你裁决")

前一版留的三笔债,本次实测确认**均维持原状**,合理 —— 它们都需要你拍板:

| # | 债 | 现状 | 卡在哪 |
|---|---|---|---|
| 1 | `all_materials.txt` 含自家生成物(第 1656 行起),是否重抽为纯原始版 | 未重抽,`PROVENANCE.md` 已标注行号 | 重抽要覆盖既有文件 |
| 2 | `D:\` 根目录 3 份源文件是否收进 `30_素材_materials\外部源\` | 未搬动 | 搬仓库外文件;SoulSync 与本项目无关 |
| 3 | 五层卡片 ↔ spec Ontology 的对齐缺口(ISSUES.md B1–B5) | 未对齐,yaml 注释已就地标注 | spec 是权威,改它须走变更评审 |

规格自身冲突(ISSUES.md A1–A5、C1)同样一字未改,只在台账登记 —— 符合硬规则。

---

## 四、真实进度(诚实版,与根 README 对齐)

- **规格**:齐全,但 10 处冲突/缺口(A1–A5、B1–B5、C1)仍全部待裁决;且存在一份过期重复副本(§二.1)。
- **知识层**:装置完好、可跑,**卡片 0 张**。`validate()` 绿 = 没东西可校验。
- **研究层**:仅设计,`hypothesis.py`/`econ.py`/`report.py`/`cli.py` 与 `agenda/hypotheses/scenarios/findings/docs/tests` **全部不存在**,README 里的命令全跑不起来(README 已诚实标注)。一条假设未登记。
- **业务代码**:零。
- **《实施理解报告》(10 问)**:材料已全读,**仍未产出**。
- **工程底座**:无 git、有悬空引用、有规格重复副本。

一句话:**文档层已经诚实且自洽,但工程底座(版本控制、单一权威源、悬空引用)还没落地,而这层比"写第一张卡片"更该先做。**

---

## 五、下一步建议(按优先级,均为低成本、不跨 WBS)

这些都属于"整理与底座",不构成跨 WBS 阶段的业务开发,可在出《实施理解报告》之前先做:

1. **P0｜`git init` + 基线提交**(§二.2)。让此后一切改动可追溯、可回滚,给"变更评审"一个载体。
2. **P0｜处置规格重复副本**(§二.1)。删或归档 `docs/impl-v1.0/`,确保权威唯一。
3. **P1｜补 `25_研究_research/docs/GUARDRAILS.md`**(§二.3)。消除唯一零成本的悬空引用。
4. **P1｜产出《实施理解报告》(10 问)**。材料已齐,这是解锁后续 WBS 的钥匙。写它时须把 ISSUES.md 的 10 处冲突作为"待裁决前提"明确列出,不能假装规格无矛盾。
5. **P2｜请你就三笔旧债 + 10 处 ISSUES 给出裁决**。尤其 A4(Outcome baseline 任务缺口)和 B3(未成年人 Modality 合规层)是实质缺口,不是措辞问题。

**建议不做**:在上述底座落地前,不要开始填卡片或写研究代码 —— 否则会在一个无版本控制、权威源不唯一的地基上累积资产。

---

## 六、本次未做的事(守界)

- 未 `git init`、未删/移任何文件 —— 底座类动作也等你确认。
- 未改 spec、未改任何 ISSUES 裁决。
- 未填任何卡片、未写任何业务/研究代码。
- 未动三笔旧债。
- 《实施理解报告》仍未产出(材料已齐,随时可写)。

---

## 七、执行记录(2026-08-09 晚,用户授权"按你的理解执行"后补)

上文 §五/§六 的建议已获授权执行。以下为已落地动作,本节使 §六"本次未做"的部分条目失效:

1. **git 化**:`git init` + `.gitignore`(忽略 `90_归档`/`__pycache__`/`.tmp`),打改动前基线提交 `4171e83`(60 文件入库,归档正确排除)。**§六"未 git init"已失效。**
2. **规格重复副本**:`docs\impl-v1.0\` 移入 `90_归档_archive\规格历史快照_impl-v1.0\` 并加"不作权威"说明;`docs\` 已空删。权威唯一。
3. **悬空引用**:补 `25_研究_research\docs\GUARDRAILS.md`(红线基线 V1)。
4. **实施方法论**:新增权威规格 `10_规格_spec\07_Family实施方法论.md`(七步 SOP + 四治理平面 + Gate 规则 + 10 处裁决)。
5. **10 处 ISSUES 裁决落地**:A4/A5/B2/B3/B4/B5 写入 `02`/`05` 原文(带「V1.1 裁决」标记);B1 落知识层代码并通过校验测试;A1/A2/A3/C1 为过程裁决,落 `07`/`05` 要点。`ISSUES.md` 顶部加裁决总表。任务总数 53→54,`manifest.json` 同步。

**仍未做**:业务/研究代码、填卡片、三笔旧债(重抽 all_materials、收编 D盘外部源、其余对齐细节)、《实施理解报告》。规格 `01`/`03`/`04`/`06` 原文本轮未改(裁决仅涉 `02`/`05`)。
