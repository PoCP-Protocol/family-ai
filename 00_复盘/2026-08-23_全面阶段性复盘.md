# 2026-08-23 全面阶段性复盘

> 复盘人:Claude Code(仓库现场唯一 Writer)。
> 方法:以运行时真相为准(`governance/TRUTH_HIERARCHY.md` 权威序),交叉核对 git/PR 实况、governance registry、架构冻结文档、代码与测试文件,不以叙述性文档判定完成度。
> 遵守 `NO_DOCUMENT_STATUS_ABOVE_RUNTIME_TRUTH`:本文只记录已核验的事实与差距,不抬高完成度。

---

## 0. 一句话结论

G0(产品+技术架构冻结)已合并 main,V4.1 架构收敛以 Draft PR#2 待架构师复核;**运行时真相未变**——35 个消费者 UI 的 `REAL_PERSISTED = 0`,真实模型外呼仅限内部 dogfood,pgvector/Redis/Temporal 均未实现。当前最大的治理事实是:**仓库并存两条治理叙事(继承自 D:\family 的 M1/M2/M3 MOS 线,与 08-22 新建的 G0→V4.1 / 35UI 线),`00_复盘/` 在此之前停在 08-11,没有任何记录覆盖 08-22 的架构转向。本次复盘补此断层。**

---

## 1. 复盘范围与本次核验的事实源

| 事实源 | 权威序 | 本次读到的关键值 |
|---|---|---|
| Git 分支 / PR | 高(GitHub observed) | 见 §5 |
| `governance/*_REGISTRY.yaml` | 高(机器 SSOT) | 授权 43 项;真实外呼仅内部 dogfood;pilot/prod = NO |
| `governance/FAMILY_35UI_RUNTIME_MATRIX_V1.json` | 高 | 35UI 运行时状态分布,见 §3 |
| `architecture/FAMILY_AI_PLATFORM_TECH_ARCHITECTURE_V4_1.md` | 中(冻结文档,2026-08-22) | 六平面 + 七域 + 13 不变量,见 §4 |
| `50_开发_dev/PROJECT_STATUS.md` | 中 | status_version 6,as_of 2026-08-11,phase M3_FAMILY_1_0_MOS |
| `README.md` / `CLAUDE.md` | 低(叙述层) | PROGRAM_MODE = M3_MOS_CLOSEOUT(可能滞后) |
| 数据库迁移 / 代码 / 测试文件 | 高(runtime) | 36 个迁移;NestJS 11 + Expo + 静态 Web;~150 测试文件 |

---

## 2. 核心发现:两条治理叙事并存(需架构师裁决)

仓库当前**同时存在两套互不完全对齐的治理框架**,是本阶段最需要澄清的风险:

### 叙事 A —— 继承线(来自 D:\family)
- 载体:`PROJECT_STATUS.md`(v6,as_of 08-11,`phase = M3_FAMILY_1_0_MOS`)、`00_复盘/`(至 08-11)、`10_规格_spec` V2.1、`20_知识_knowledge/evidence.py`。
- 于 **2026-08-20 `8eee065` "initialize family-ai from full family workspace"** 整体导入,是本仓库的起点快照。
- 语汇:M1 Family Core / M2 Wave / M3 MOS / W2R / FPAI runtime admitted to MOS。
- 真实工程资产在此线沉淀:M1 family-core 真实 PG + HTTP E2E、Principal/AI Gateway 包、evidence.py。

### 叙事 B —— 新 family-ai 线(2026-08-22)
- 载体:PR#1(G0 freeze,**已合并** `afabfc1`)、PR#2(V4.1 convergence,**Draft** `7959929`)、`FAMILY_35UI_RUNTIME_MATRIX_V1.json`、`FAMILY_AI_PLATFORM_TECH_ARCHITECTURE_V4/V4_1.md`。
- 语汇:G0/G1-A/B/C/D 门禁 / 35UI / 六平面 / 七域 / 13 不变量。

### 张力点
1. **两套阶段词汇未桥接**:`PROJECT_STATUS.md` 仍称 `M3_MOS_CLOSEOUT` 为当前 PROGRAM_MODE,而 08-22 之后的实际执行走的是 G0→V4.1 门禁。README 的 PROGRAM_MODE 是滞后的。
2. **`00_复盘/` 有 12 天空白**:最后一份是 `2026-08-11_文档与实际进度核对.md`,其后发生了 G0 合并、main 保护、仓库转 private、V4.1 收敛——均无复盘记录。
3. **RUNTIME_MATRIX 文件形态存疑**:第一次探查未在 governance 下找到 `FAMILY_35UI_RUNTIME_MATRIX.json` 的单一权威文件,存在 `FAMILY_35UI_PROGRAM_V1.yaml` 等多份 YAML/MD;需架构师确认哪一份是机器 SSOT。

> **裁决请求(P0)**:请架构师明确 `PROJECT_STATUS.md`(M3 MOS 线)与 V4.1/35UI 门禁线的从属关系——是 M3 MOS 收编入 G0/G1 门禁,还是两者并行各自 close-out?在此之前,PROGRAM_MODE 以哪份文件为准?

---

## 3. 运行时真相(诚实版)

### 3.1 35 个消费者 UI 运行时状态分布(来自 RUNTIME_MATRIX,与 governance 记忆一致)

| 运行时状态 | 数量 | 含义 |
|---|---:|---|
| `REAL_PERSISTED` | **0** | 无一个 35UI 消费者页面有真实持久化写闭环 |
| `READ_ONLY_PROJECTION` | 18 | 只读投影 |
| `TEST_LOOP_FIXTURE` | 8 | 闭环 fixture,非真实效应 |
| `GATE_BOUNDARY` | 6 | 受 Gate 控制(涉真人/儿童/支付):UI-02/03/10/17/25/30 |
| `LOCAL_DRAFT` | 2 | 本地私有草稿:UI-05/12 |
| `NOT_IMPLEMENTED` | 1 | UI-27(家庭小记详情) |

> ⚠️ 注意区分:**M1 family-core 后端**确有真实 PG + E2E(TASK-101~106),那是后端能力;但 **35 个面向家庭的产品 UI 层 `REAL_PERSISTED = 0`**。`35_UI_BACKEND_COMPLETE = NO`。两者勿混淆。

### 3.2 技术栈实况

| 组件 | 声称 | 代码实况 |
|---|---|---|
| API | NestJS 业务真相 | ✅ NestJS 11.x(`apps/api`) |
| Web | React19 前端 | ⚠️ 实为静态 HTML/JS 投影(`tools/serve-static.mjs`),非标准 React SPA |
| Mobile | 存在 | ✅ Expo 54 + expo-router + Drizzle ORM + tRPC(`apps/mobile`)——含**待 G1-B 移除的客户端直连提供商路径** |
| DB | PG18 + Outbox | ✅ 36 个迁移;Outbox(`0030 family_product_event_envelope`)、LLM 网关审计(`0021`)、model attempts 账本(`0014`)已落。⚠️ **PG18 升级、pgvector、Redis、Temporal 均无 SQL/依赖痕迹 = NOT_IMPLEMENTED**(与门禁一致,属 G1-C 范围) |
| 模型网关 | FamilyLlmGateway | ✅ ai-gateway 包已适配 Anthropic / Zhipu / Fake + routing/failover;真实外呼仅内部 dogfood(profile=model_first_internal),pilot/prod = NO |

### 3.3 测试实况
- ~150 个测试文件(99 `.spec.ts` + 34 `.test.ts` + 38 `.e2e-spec.ts`),框架 Vitest 2.1.5。
- `.skip` / `.only` / `it.todo` = **0**;fixture 显式版本化,非全局 mock。
- 集成测试对真实 PostgreSQL 校验(outbox 事件计数、事务隔离)。
- ⚠️ `@family/contracts` 需先 build 出 dist,否则 api vitest 报 packageEntryFailure。

---

## 4. 架构冻结现状(V4 → V4.1)

- **V4(G0)已合并 main**,V4.1 以 `FAMILY_35UI_FULLSTACK_ARCHITECTURE_V1.md` 标注 `SUPERSEDED_FOR_EXECUTION_BY = V4_1`,执行权已转移至 V4.1。
- **六平面**:Experience / Interaction-API / Domain-Truth(NestJS+TS+PG,只经 Named Action 写)/ Workflow-Event(Outbox/Temporal)/ AI-Intelligence(Python 侧车,只返 Hypothesis/Draft)/ Data-Knowledge(PG/pgvector/S3/Redis)+ 横向控制平面(Identity/Tenant/Consent/Safety/Audit/OTel)。
- **七域**:FAMILY_CORE / GROWTH_INTELLIGENCE / GROWTH_JOURNEY / RESOURCE_NETWORK / SERVICE_OS / COMMERCE_ENTITLEMENT / CONTENT_COMMUNITY(Resource 与 Commerce 已拆分,FAMILY_CONTEXT 降为跨域平台不拥有 canonical 写)。
- **13 条永久不变量**已冻结(NO_SECOND_BUSINESS_BACKEND / NO_CLIENT_DIRECT_MODEL_CALL / NO_AGENT_DIRECT_DATABASE_WRITE / NO_DOCUMENT_STATUS_ABOVE_RUNTIME_TRUTH / PLATFORM_MARGIN_RANKING_SIGNAL=0 等)。

---

## 5. Git / PR / 分支现状(2026-08-23)

- 当前分支:`architecture/family-ai-v4-1-convergence-001`,**ahead main 3**,与 origin 同名分支 IN_SYNC(0/2 仅本地 pnpm-lock 未提交改动)。
- **PR#1**(G0 35UI full-stack baseline)= **MERGED**(`afabfc1`)。
- **PR#2**(V4.1 convergence)= **DRAFT**,标题含 "architect review required" —— `AUTO_MERGE=NO`,待架构师按精确 HEAD 复核后合并。
- 分支盘点:`main` / `platform/35ui-fullstack-rebaseline-001`(已并) / `platform/ai-platform-foundation-g1a`(旁支草案,不并) / `rescue/35ui-pre-g0-20260822`(pre-G0 快照,已推) / `feat/receipt-assignment-batch-metrics`(仅 origin)。
- 治理:main 已保护(require PR + 1 approval + green `alignment` check + 禁 force-push + enforce_admins);仓库已转 private。
- 工作树:仅 `50_开发_dev/pnpm-lock.yaml` 有未提交改动(需甄别是否提交)。

---

## 6. 未决债与风险台账

| # | 事项 | 级别 | 现状 | 处置建议 |
|---|---|---|---|---|
| D1 | 两套治理叙事未桥接(M3 MOS vs G0/V4.1) | P0 | 并存,PROGRAM_MODE 口径不一 | 架构师裁决从属关系(§2) |
| D2 | `00_复盘/` 12 天断层 | P0 | 本文补记 | 以后每次门禁事件当日追加复盘 |
| D3 | PR#2(V4.1)待架构师复核合并 | P0 | Draft,门禁绿(arch v4.1 PASS / 35ui PASS / contract 6/6 PASS;strict 仅 4 项已知运行时债) | 架构师给精确 HEAD → rebase 最新 main → alignment 绿 → approve |
| D4 | G1-B(runtime convergence)HOLD | P1 | 硬门 HOLD_V4_1_NOT_MERGED | 待 PR#2 合并后跑 |
| D5 | G1-C(data & workflow foundation)链式 HOLD | P1 | 硬门要求 V4.1 + G1-B 先合并;需本机 docker 起 PG18/Redis/MinIO/Temporal 才能验收 | 排在 G1-B 之后 |
| D6 | 35UI `REAL_PERSISTED=0` | P1 | 产品面无真实写闭环 | G1 之后按域推进纵切 |
| D7 | Mobile 客户端直连模型提供商路径未删 | P1 | 代码仍在,运行时被 bearer flag 降级 | G1-B 移除;代码宜标注 RUNTIME_DEBT_FOR_G1B |
| D8 | RUNTIME_MATRIX 机器 SSOT 文件形态不清 | P2 | 多份 YAML/MD 并存 | 架构师指定唯一权威文件 |
| D9 | 商业假设未证伪(25_研究 BM 线 0 supported) | P2 | Pilot 前置缺失 | Pilot Gate 前置补首次裁决 |
| D10 | ISSUES V2.1 的 B4/B5 契约冻结、A3 Python 边界落地 | P2 | 登记未落地 | 进 G1 业务域前核实 |

---

## 7. 下一步(按优先级,均需 owner/架构师授权后执行)

**P0(治理澄清,不写代码)**
1. 请架构师裁决 §2 的两叙事从属关系,统一 PROGRAM_MODE 口径。
2. PR#2(V4.1)复核合并流程走通(D3)。

**P1(合并后解锁)**
3. PR#2 合并 → 跑 G1-B(需先 `@family/contracts` build dist)→ 移除 mobile 直连路径。
4. G1-B 合并 → 架构师给 G1-C 的 authorized-base-sha → 本机 docker 起全套基础设施验收。

**P2(范围聚焦)**
5. 进业务域纵切前,冻结 B4/B5 契约、确认 A3 Python 侧车边界。
6. 启动 25_研究 BM 线首次假设登记(Pilot 前置)。

---

## 8. 纪律自查(本次复盘遵守情况)

- ✅ 以 runtime/DB/GitHub/registry 为准,未以 README 判定完成度。
- ✅ 未合并 / 未推送任何分支;未改架构冻结文档与规格原文。
- ✅ 本文追加至 `00_复盘/`,不覆盖旧复盘。
- ✅ 诚实标注 `35_UI_BACKEND_COMPLETE=NO`、`REAL_PERSISTED=0`、pgvector/Redis/Temporal 未实现。
