# 文档 ↔ 实际开发进度 核对（2026-08-11）

目的:把"文档描述"与"实际已提交的开发进度"对齐。**以 git 已提交的 HEAD 为实际进度的唯一权威**(工作树因多会话并行处于混合态,不可信)。
方法:读 HEAD 提交历史 + `git show HEAD:PROJECT_STATUS.md` + HEAD 的 reports 清单。**未做任何提交/reset/restore**,以免破坏其他会话未提交的成果。

---

## 一、实际进度(权威 = 已提交 HEAD)

- **phase**:`M2_WAVE_3_OBSERVE_AND_REVIEW`;**milestone**:`PHASE_B_PRODUCT_CLOSURE_IN_PROGRESS`(PROJECT_STATUS status_version 3,as_of 2026-08-10)。
- 已 CLOSED / PASS(节选自提交历史与 gate 报告):
  - **M1 Family Core**:TASK-101→107 全部完成,`M1 ... CLOSED`(见 `reports/M1_FAMILY_CORE_REPORT.md`、`TASK-10x_*_GATE.md`、`SPRINT_0_FINAL_GATE.md`)。
  - **Rebaseline V3.0 / V3.2 SSOT**:产品垂直切片优先、前端/UX 列为一等交付线。
  - **M2**:Wave1 UNDERSTAND CLOSED;Wave2 Contract Freeze PASS;**Wave2 真实 PostgreSQL HTTP E2E = 55 tests PASS**;M2-101/102/103/104/105 完成;Wave3 Observe&Review + 产品收尾进行中。
  - 迁移已至 `0001…0010`;另有 FELS legacy-system、Principal AI/FPAI、we-are-family 等产品线报告数十份。
- 结论:项目**远超**本会话此前的心智模型(我停留在 Sprint 0 / TASK-002)。

## 二、文档 ↔ 实际 的偏差(需统一处)

1. **committed PROJECT_STATUS 基本反映实际**(status_version 3 / M2 Wave3)——文档层面是当前的,**无需重写**。
2. **工作树里的 PROJECT_STATUS 是过时回退版**(退回到 "M2-101 / AI Development OS V1.0" 时代),与 HEAD 冲突 → 属工作树污染,应 `git restore` 回 HEAD。
3. **本会话(08-09 视角)产出的 TASK-002 / TASK-002R 叙事已过时**:所谓 "BLOCKER-01 迁移非法 / READY_FOR_TASK_101=NO/YES" 均为历史——Sprint 0 早已 `SPRINT_0_FINAL_GATE` 收口,真实 PG E2E 早已达成。**已删除本会话本回合新增的过时未跟踪产物**(`reports/task-002r/`、`reports/ENGINEERING_CONTRACT_REVALIDATION.md`),以免误导。
4. 迁移文件 `0001-0003` 工作树版本与 HEAD 一致(无 diff),不影响。

## 三、工作树危险状态(未处理,交人工/活跃会话)

`git status`:**61 个已修改(M)+ 大量未跟踪(??)**,是两类混合:
- **其他会话未提交的真实成果**:`products/famili-principal/*`、`products/we-are-family/*`、`legacy-system/apps/api/src/pg-fels-repository.ts`、`apps/api/src/modules/family/*.ts`、`apps/web/*`、wf1 research artifacts 等。
- **本会话的过时回退残留**:`packages/contracts/src/index.ts`(比 HEAD 少 332 行)、`apps/api/src/main.ts`(-6)、`package.json`、`PROJECT_STATUS.md`(退回旧版)。

> **警告:此刻任何 `git commit / reset --hard / stash / restore .` 都可能① 用过时内容覆盖已提交的新成果,或② 丢失其他会话尚未提交的成果。本会话已停止一切写库/git 变更操作。**

## 四、建议的统一步骤(由单一活跃会话/人工在其环境执行)

1. **确立单一提交所有权**:今后由一个会话负责 git 提交,避免多会话共享工作树互相污染。
2. **甄别工作树**:对上面"本会话过时回退残留"逐个 `git diff HEAD -- <file>` 确认是回退,再 `git restore --source=HEAD -- <file>` 复原(至少含 `packages/contracts/src/index.ts`、`PROJECT_STATUS.md`、`apps/api/src/main.ts`、`package.json`)。
3. **保全并提交其他会话真实成果**:确认 `products/*`、`legacy-system/*`、`apps/*` 的 M/?? 是有效新成果后,由活跃会话正常提交。
4. **文档统一基线**:以 committed `PROJECT_STATUS.md`(status_version 3)为 SSOT;`00_复盘`、`10_规格_spec`(V2.1)、`50_开发_dev/reports` 已提交部分即为准。
5. 本会话历史价值:早期 git 化、V2.1 规格权威化、AI Dev OS V1.1 引入、Sprint0 起步——这些已并入后续提交,无需再动。

## 五、本会话本回合的最终动作
- **未** commit、**未** reset/restore、**未**改任何已跟踪文件。
- **仅**删除本会话本回合新增的过时未跟踪产物,并新增本核对报告(新文件,不覆盖任何内容)。
- 交回控制权,等待你决定由哪个会话来做工作树清理与提交。
